/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental: {
  //   appDir: true,
  // },
  images: {
    domains: [
      'www.google.com',
      'flagcdn.com',
      'flagsapi.com',
    ],
  },
};
export default nextConfig;
